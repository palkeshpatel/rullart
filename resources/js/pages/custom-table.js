/**
 * Custom Table Page
 * This file is required for pages that use custom table functionality
 * Note: app.js is already loaded via @vite directive in the layout
 */

// Add any custom table-specific initialization here if needed
document.addEventListener('DOMContentLoaded', function() {
    // Custom table initialization code can go here
    // All base functionality from app.js is already available
});

